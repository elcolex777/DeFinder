
Добавить кнопку "Загрузить".

Разместить под кнопкой Сохранить.
При нажатии должен открывать диалог выбора картинок из галереи (с диска).
Выбрать можно больше одного файла.
После выбора картинок, должна выполняться логика как при нажатии кнопки Сохранить (то есть получить маски, и отправить запрос в очередь на сохранение) только для каждого выбранного файла последовательно. На странице прогресс должен отображаться в том же блоке который используется для кнопки Сохранить.


***

# Доработать процедуру запуска поиска.

При создании нового снимка или выбора из галереи после получения масок, запускать поиск как это делается при выборе маски, но только вместо кропа маски использовать оригинальное изображение.

# Доработать отображение блока с картинкой, который открывается при клике на картинку в результатах поиска.
1. наложить на картинку квадрат размером 20 на 20 пикселей с центром заданным координатами из свойства center (элемента из результатов поиска)
Описание этого поля на бэкенде: center: Optional[dict] = Field(None, description="Координаты центра маски {x, y}")
координата центра задаются в координатной системе отображаемой картинки (то есть /api/v1/{image_path}).
Граница квадрата должна быть толщиной 2px зеленого цвета.

# Доработать отображение элемента результата поиска.
Нижняя граница (border) картинки должна быть толщиной 5 пикселей и случайным цветом. Но цвет должен быть одинаковым для элементов у которых свойство image_path результата поиска совпадает.

***

Проанализируй исходный код (html+js) браузерного приложения для поиска вещей по картинке. Надо подправить код отображения скора в результатах поиска:
1. Сделать вывод значения score в формате ##%. То есть выводить в целых процентах без разделителя.
2. Уменьшить шрифт до 15 pt

***

Маски, накладываемые на изображение, сделать кликабельными. 
При клике на конкретную маску (закрашенную область), она должна стать выбранной. Все остальные маски становятся невидимыми.
При повторном клике на ту же маску или в любом другом месте на картинке, эта маска становится не выбранной, а остальные маски становятся видимыми (как и текущая).

При выборе маски нужно выполнить следующие действия:
1. Сделать кроп изображения по границам этой маски, масштабируя координаты на оригинальное изображение.
2. Если полученный кроп изображения больше по ширине или высоте 200 пикселей, то уменьшить это изображение так чтобы самая большая из сторон (ширина или высота) изображения была ровно 200px, сохраняя оригинальные пропорции.
3. Преобразовать изображение в base64.
4. Отправить POST-запрос на бэкенд /api/v1/masks/search передав туда ил пользователя ипреобразованное изображение.
Входные параметры (модель на бэкенде):
class SearchRequest(BaseModel):
    user_id: UUID4 = Field(..., description="GUID пользователя")
    image_base64: str = Field(..., description="Изображение для поиска в Base64")

5. Внизу страницы показать блок результатами поиска.
В заголовке картинка из пункта 3 в теге img, у которой src задан в base64 и фиксированная ширина 50 пикселей. Справа индикатор отправки запроса (отправка/успешно получено/ошибка).
6. После получения ответа от бэкенда, в блоке результатов поиска показать список элементов с результатами.
Формат ответа от бэкенда следующий:

class SearchResultItem(BaseModel):
    image_path: str
    crop_path: Optional[str] = Field(None, description="Путь к файлу кропа маски")
    center: Optional[dict] = Field(None, description="Координаты центра маски {x, y}")
    score: float = Field(..., description="Косинусное сходство (от -1 до 1, чем выше — тем ближе)")

class SearchResponse(BaseModel):
    results: List[SearchResultItem]

return SearchResponse(results=search_results)

список нужно отобразить в две колонки.
каждый элемент списка показать в виде квадратной картинки. Внутри должны сохраняться пропорции картинки. Ссылка для картинки в свойстве crop_path. Формат атрибута src: /api/v1/{crop_path}
В правом нижнем углу картинки наложить текст, взятый из свойства score. Размер шрифта 20pt. Формат числа 00.00
При клике на картинку показать блок с картинкой image src=/api/v1/{image_path} под заголовком блока результатов поиска.

***

сделать значение ползунка Размер изображения 800 поумолчанию 
сделать блок с настройками раскрывающимся. По умолчанию он скрыт.
Для блока сделать заголовка Настройки

***

Добавить обработчик для статических файлов расположенных по адресу:
data/{user_id}/images

адрес из вне: /api/v1/data/{user_id}/images

например, должны обрабатываться такие url:
/data/ed72490a-d06a-4ce1-b4cd-07167d045292/images/f7f3e136-0a4c-47c3-8a52-b8e667e1bf8c.jpg
/data/ed72490a-d06a-4ce1-b4cd-07167d045292/images/65d400ff-9fec-49de-8a63-40a931e2849/crop_4.jpg

Доработать обработчик search_image

В формате ответа в списке найденных элементов надо добавить значения center и crop_path из методанных.

***

Делать отправку запросов на сохранение через очередь (переменная в памяти).
Каждое нажатие на кнопку Сохранить добавляет задачу в очередь. Очередь обрабатывается последовательно. 
Внизу страницы добавить блок со списком обрабатываемых задач на сохранение. Каждый элемент списка отображает статус (В очереди/Обрабатывается/Успешно Сохранено, Ошибка). Если статус Ошибка, то разместить в этом элемене кнопку Повторить, при нажатии делается попытка отправить запрос еще раз.
При успешном сохранении элемент в списке отображается 5 секунд и исчезает из списка.

***

Добавить кнопку Сохранить (под кнопкой Выбрать из галереи)
Кнопка доступна только если получены маски или если запрос /api/v1/masks/predict завершился успешно и не выполняется.
При нажатии на кнопку отправляется POST запрос в бэкенд по адресу /api/v1/masks/index

Параметры запроса (код из бэкенда):
class IndexMasksRequest(BaseModel):
    user_id: UUID4 = Field(..., description="GUID пользователя")
    image_base64: str = Field(..., description="Изображение в формате Base64")
    masks: List[List[int]] = Field(..., description="Массив RLE-масок из predict_masks")

Параметры ответа (код из бэкенда):
    return {
        "status": "ok", 
        "message": f"Успешно добавлено объектов в индекс: {len(crop_images)}."
    }

В параметре user_id передавать ид текущего пользователя
В параметре image_base64 передавать оригинальную картинку (с реальной шириной/высотой) закодированную в base64
В параметре masks передавать массив масок полученных, ранее после отправки запроса /api/v1/masks/predict.
Надо определить в каком разрешении (ширина/высота) маски были получены ранее при отправке /api/v1/masks/predict.
Значения координат масок надо масштабировать учитывая ширину/высоту оригинального изображения. Список массок надо передавать сжатым методом RLE (по аналогии с разжатием как сделано при обработке массок от /api/v1/masks/predict)

***

 На вход бэкенда будет передаваться ид пользователя.

вот пример поля на питоне на стороне бэкенда:

user_id: UUID4 = Field(..., description="GUID пользователя")


Необходимо при загрузке страницы проверить значение параметра строки url user_id, если его нет, то сгенерировать новый guid и прописать в строке url браузера.


Далее значение этого параметра будет использоваться для взаимодействия с бэкендом в некоторых запросах


***

на основе кода ниже сделать пример fastapi сервиса со следующими конечными точками:

1. получить маски по содержимому изображения. На входе изображение в base64 и параметры для mobilesam (с установленными значениями по умолчанию). На выходе массив масок.

2. сохранить маски и проиндексировать. На входе массив масок, изображение в base64 и id пользователя (строка guid). для каждого пользователя (id) создается отдельный индекс faiss на диске. путь на диске data/{id пользователя}/index/faiss.index
изображение сохраняется на диск в подпапку data/{id пользователя}/images/guid.{ext}
путь к изображению также сохраняется в методанных.
На выходе результат ок.

3. поиск по фото. На входе изображение в base64, id пользователя.
На основе входного изображения делаем вектор и ищем в индексе faiss. 
на выходе возвращает массив из 10 найденных объектов вместе с image path и score




pip install ultralytics torch torchvision open_clip_torch faiss-cpu pillow numpy



import torch
import numpy as np
from PIL import Image
import faiss
from ultralytics import SAM
import open_clip

# ==========================================
# 1. ИНИЦИАЛИЗАЦИЯ МОДЕЛЕЙ
# ==========================================
# Загружаем MobileSAM через Ultralytics
sam_model = SAM("mobile_sam.pt") 

# Загружаем CLIP для извлечения качественных векторов (эмбеддингов)
clip_model, _, preprocess = open_clip.create_model_and_transforms('ViT-B-32', pretrained='laion20c_e32')
clip_model.eval()

# Создаем индекс FAISS для поиска по вектору (размерность 512 для ViT-B-32)
dimension = 512
index = faiss.IndexFlatIP(dimension)  # Индекс для Inner Product (Косинусное сходство при нормализации)

# Списки для хранения метаданных
saved_vectors = []
image_metadata = []

# ==========================================
# 2. ФУНКЦИЯ СЕГМЕНТАЦИИ И ВЕКТОРИЗАЦИИ
# ==========================================
def process_and_index_image(image_path, object_id, prompt_points=None):
    """
    Сегментирует объект, вырезает его, превращает в вектор и добавляет в базу.
    prompt_points: список координат [[x, y]] для выбора объекта. Если None — берется авто-маска.
    """
    img = Image.open(image_path).convert("RGB")
    
    # Шаг 1: Сегментация с помощью MobileSAM
    if prompt_points:
        results = sam_model(image_path, points=prompt_points, labels=[1])
    else:
        results = sam_model(image_path) # Автоматический режим (сегментирует всё)
        
    if not results or len(results[0].masks) == 0:
        print(f"Объекты на {image_path} не найдены.")
        return

    # Берем первую найденную маску
    mask = results[0].masks.data[0].cpu().numpy()
    
    # Шаг 2: Изоляция объекта (наложение маски на изображение)
    img_np = np.array(img)
    # Масштабируем маску под размер оригинального фото, если они различаются
    mask_resized = np.array(Image.fromarray(mask).resize(img.size, resample=Image.NEAREST))
    
    # Создаем изображение, где всё кроме маски — черное
    isolated_object = np.zeros_like(img_np)
    isolated_object[mask_resized == 1] = img_np[mask_resized == 1]
    isolated_img = Image.fromarray(isolated_object)

    # Шаг 3: Получение вектора через CLIP
    image_input = preprocess(isolated_img).unsqueeze(0)
    with torch.no_grad():
        image_features = clip_model.encode_image(image_input)
        # Нормализуем вектор (обязательно для косинусного сходства в FAISS)
        image_features /= image_features.norm(dim=-1, keepdim=True)
        vector = image_features.cpu().numpy().flatten()

    # Шаг 4: Добавление в FAISS
    index.add(np.array([vector]).astype('float32'))
    image_metadata.append({"object_id": object_id, "path": image_path})
    print(f"Успешно проиндексирован объект из {image_path}")

# ==========================================
# 3. ФУНКЦИЯ ПОИСКА ПОХОЖЕГО ОБЪЕКТА
# ==========================================
def search_similar_mask(query_vector, top_k=3):
    """Ищет top_k похожих векторов в базе FAISS"""
    query_vector = np.array([query_vector]).astype('float32')
    distances, indices = index.search(query_vector, top_k)
    
    print("\n--- Результаты поиска ---")
    for i, idx in enumerate(indices[0]):
        if idx == -1: continue
        score = distances[0][i]
        meta = image_metadata[idx]
        print(f"Место {i+1}: ID={meta['object_id']}, Файл={meta['path']}, Сходство={score:.4f}")

# ==========================================
# ПРИМЕР ИСПОЛЬЗОВАНИЯ
# ==========================================
# 1. Заполняем базу вашими фото (пройдите циклом по папке)
# В prompt_points передаем координаты центра нужного объекта [X, Y]
process_and_index_image("photo_1.jpg", object_id="apple_red", prompt_points=[[250, 300]])
process_and_index_image("photo_2.jpg", object_id="banana_yellow", prompt_points=[[100, 150]])

# 2. Допустим, у нас есть новое фото (запрос), из которого мы извлекли вектор query_vector
# (Для теста вызовем векторизацию отдельно или возьмем вектор существующего объекта)
# Сканируем тестовое изображение, чтобы получить вектор для поиска:
test_img = Image.open("query_photo.jpg").convert("RGB")
test_input = preprocess(test_img).unsqueeze(0)
with torch.no_grad():
    query_feat = clip_model.encode_image(test_input)
    query_feat /= query_feat.norm(dim=-1, keepdim=True)
    query_vector = query_feat.cpu().numpy().flatten()

# 3. Ищем похожие маски в нашей базе
search_similar_mask(query_vector, top_k=2)
